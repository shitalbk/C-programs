#include <stdio.h>
int main()
{
	int i,j;
	char line[20];
	printf("Enter a line of text: ");
	gets(line);
	printf("Enter the substring you want to search: ");
	gets(line);
	for(i=0;line[i]!='\0';i++)
	{
		
	}
}
